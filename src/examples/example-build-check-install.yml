description: >
  Simply build, check and test installation of R package.

usage:
  version: 2.1
  orbs:
    r-orb: knightdave/r-orb@1.0.0
  workflows:
    simple-build:
      jobs:
        - r-orb/build-check-install
