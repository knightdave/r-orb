description: >
  Build and check your R project with pkgdown build.

usage:
  version: 2.1
  orbs:
    r-orb: knightdave/r-orb@1.0.0
  workflows:
    simple-build:
      jobs:
        - r-orb/build-check-install
        - r-orb/pkgdown

