description: >
  Build and check your R project with lintr.

usage:
  version: 2.1
  orbs:
    r-orb: knightdave/r-orb@1.0.0
  workflows:
    simple-build:
      jobs:
        - r-orb/linter
        - r-orb/build-check-install
