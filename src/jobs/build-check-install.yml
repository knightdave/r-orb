description: >
  Lint R project with lintr

executor: default

# parameters:
#   path:
#     type: string
#     default: "."
#     description: "Path to run lint"
steps:
  - checkout
  - r-build
  - r-check
  - r-install
